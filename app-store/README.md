# C&R Enterprise App Distribution Portal

* created by "create-react-app" package

# Build
* `npm install` and `npm run build`
* output in `build` folder

# Usage
* `node encrypt json/source.json > json/app.json`
* upload the encrypted app.json file on blob
